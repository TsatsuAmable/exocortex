[project]
name = "aineko-delivery-controller"
version = "0.1.0"
description = "Local, model-agnostic durable delivery-job controller"
requires-python = ">=3.11"

[project.scripts]
aineko-delivery = "delivery:main"
